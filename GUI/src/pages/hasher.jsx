import React from 'react';
export const Hasher = props => (
    <span className="hasher">
        {
            //props.password.split("").forEach(x => <h2>*</h2>)
        <h3> ****</h3>
        } 
    </span>
);