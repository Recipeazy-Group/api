//only .js because it's a class, not a component
export class MoreInfoCard {
    constructor(topicName, moreInfo, image) {
        this.topicName = topicName;
        this.moreInfo = moreInfo;
        this.image = image;
        
    }
}